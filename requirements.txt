# Unified project dependencies.

# Core runtime
numpy>=1.26,<3
soundfile>=0.12
scipy>=1.11

# Demo UI
gradio>=5.0,<6
streamlit>=1.36,<2
pandas>=2.0,<3
socksio>=1.0

# Development and checks
pytest>=8
ruff>=0.5
mypy>=1

# Optional production model backends. The code imports these lazily, and model
# weights are downloaded separately by each backend at runtime.
openai-whisper>=20231117
faster-whisper>=1.0
whisperx>=3.1
pyannote.audio>=3.1
funasr>=1.0
speechbrain>=1.0,<2
huggingface_hub>=0.23
openai>=1.0
