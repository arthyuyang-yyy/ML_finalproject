# Store large datasets outside the iCloud-synced repository.
MEETING_DATA_ROOT=/absolute/path/to/meeting-memory-data

# Optional model credentials used only when their backends are enabled.
# HF_TOKEN=
